# Coyote Mountain Studio — session handoff, night of 11–12 September 2026

Written at Will's stopping point, 12 September. **Read this first**, then `HANDOFF-2026-09-12-session.md` for the 8–12 September work it follows, then `HANDOFF-2026-09-08-render-paint-sandbox.md` items 30–50 for older code-level detail. Memory: `project_coyote_studio.md`, `reference_hooke_park.md`.

## Where things live

- **Repo** `C:\Users\zolar\WebDev\coyote-studio` = public GitHub `shopnerd/coyote-mountain`, Pages at https://coyotemountainfarm.com. `topo.html` is the tool; `flight.html`, `index.html`, `teachers.html`, `walk/`, `projects/centro-equino.json`, `home.json`, `tools/`.
- **Artifacts**: topo copy https://claude.ai/code/artifact/062c2198-f39e-45bc-aa65-6025b1e7b42c (republish with `url` after every push, outer document tags stripped) · section style board https://claude.ai/code/artifact/f4c00b13-c082-4374-b0a1-bc2043785aac · Hooke Park brief https://claude.ai/code/artifact/0340d171-4614-423e-9d14-92b5229e08bd · plan https://claude.ai/code/artifact/40b48406-3be9-4d5f-9b1f-24fb3d019609
- **This session's scratchpad**: `C:\Users\zolar\AppData\Local\Temp\claude\C--Users-zolar-WebDev\1ae08b44-2083-42ad-a09f-64eeaeb6740a\scratchpad`. Tests e2e160–e2e174, patches 110–116, `dump-profile.js` + `profile.json` (the Encino Solo A–B section), `section-styles.src.html` (the style board's source), `board-shots.js`, `check-mesh.py`.
- **Points file**: `Google Drive\MEXICO (1)\DroneDeploy\coyote-studio-import\encino-solo-points.bin` (1.02 M points, 8.2 MB).

## Standing rules (unchanged, plus what tonight added)

- Single-file vanilla JS + Canvas, no libraries, Will's light house style, plain English, no native tooltips, never name other programs in the page.
- Test locally with Playwright against `python -m http.server 8765` in the repo (it dies between turns, restart it), then push, poll the live page with curl, republish the artifact, show visual proof, update memory + handoff.
- Keys never in the repo, never handled by Claude.
- Never append `//` to a one-line statement chain in topo.html.
- **New: `topo.html` in the working copy is LF, git stores CRLF.** Every patch script must detect the newline (`nl = '\r\n' if '\r\n' in s else '\n'`) or `str.index(nl, …)` throws.
- **New: a second session shares this repo.** "Design mode" (`design-mode.js`, `?design`) committed three times tonight between 00:37 and 01:21, and its loader line in topo.html rode into commit `6298177` by accident. `git status` and `git log -3` before patching; never blind-`git add` topo.html.
- **New: never use canvas `filter` per `drawImage`.** Tried for tile shading; the page stalled over 30 s.
- Will dictates; read for intent.

## What was built, in order

1. **Animated section (`dd2688c`), then rebuilt as a score (`5bd6577`).** `animate` beside `paint this section` opens `#animPanel`. Will's verdict on the first cut: *"needs work, potential … simple but pedestrian"*, and he asked for style thumbnails. Board built (9 live directions on the real A–B profile) → he picked **8 and 9**: Halprin-style notation + risograph. The panel now draws the section above (blue halftone earth, existing dashed, cut as dense dots, fill as ember hatching) and below it the day as a score with a playhead: **sun · shade · walk · ground**, past in full ink, future faint. Two plates (ember `#e0552b`, blue `#2f5fa8`) multiplied on grained paper, ±1.3 px out of register. Still parts cached in `ANIM.plates.a/b` by `animStatic()`, moving parts on `da/db`. `drawSectionOne` now returns its frame including `pw` (its absence silently emptied every loop the first time). Sound = WebAudio wind. Save video = one loop as webm (mp4 on Safari).
2. **Birds, and their removal (`44facf9`, then `5808c4a`).** Built a simulated murder of 26 crows with depth, flock wheel, flap/glide/bank, three-quarter rear silhouettes (a side view read as fish). Will: *"animation looks terrible … just remove the birds"*. Gone: crows, the birds row, the chirps. The wind stays. **Parked brief:** crow caws instead of chirps + a Merlin-style spectrogram of them overlaid on the section graph. **Do not iterate on the animation without his references.**
3. **Walk hidden (`41cc1de`).** Will: *"take walk off until I can test it offline."* The `walk` button carries `style="display:none"`; all walk code intact; tour and painted-ground toast no longer mention it. Restore = drop the attribute and the two sentences (see the diff).
4. **Photo ground seams, fixed properly (`db04f7d`).** Will's screenshot still showed a pale lattice. Painting the under-fill red proved the fill innocent: each tile's shade polygon was being painted over by the *next* tile's 5% patch overlap, i.e. raw unshaded photo over a darkened rim. Fix: **no patch overlap** (`ov = 0`), the fill under each patch darkened to the tile's own shade, shade polygon outset .3 px. Seam score at dpr 2: 13.4 → 5.4 per 1000; render time unchanged (182 ms per 1536×864). `__dbg` gained `aerialCache`, `AER`.
5. **Photo opacity on the model (`6298177`).** Will: *"add slider under aerial photo button on model page."* `#aerialOp` travels with `#aerialRow` in `setMode`. On the model it now does real work for the first time: each textured tile blends toward the paper tone by `S.aop` and lays the paper tone over the patch at `1 − aop`; `modelFrame`/`pipFrame` keys carry `S.aop`.
6. **Colour vs photo (`5bd6577`).** Will: *"when you select a button and nothing happens it feels weird."* Picking elevation/slope/aspect/cut·fill with the photo or painted ground on turns them off with a toast; picking the photo puts the colour back to paper.
7. **Sculpt and draw split (`6c89255`).** `#sculptSec` (raise, lower, smooth, flatten + `#sculptSlot`) shows on plan and model; `#brushSec` (pen and the plan tools) on plan only. The slider block `#brushParams` moves into whichever block owns the chosen tool. Tour stops retargeted. **The tour docx with Will predates this** (two card texts changed).
8. **3d model export menu (`aa50c0c`).** Will: *"I love how rhino will open and save most any file type. Maybe we should have a drop down."* `#meshFmt`: **stl · obj + mtl + png (zipped) · glb (one file, metres, y up) · wrl (VRML, zipped)**. Shared `modelParts()` (uv per grid node), `modelTexture()` (painted ground → photo → surface colour at 4×), `zipStored()` + `crc32()`. **Bug found on the way:** since trim-to-lot, `tin()` returned true outside the grid, so the full-site STL had **no side walls** (59,004 tris, not watertight). Fixed in `stl0` and `modelParts` (59,996). Not yet opened in Will's CAD.
9. **iPad pass + home-screen mode (`4ab0a5f`).** Emulated iPad both orientations with real touch (e2e170): layout holds, touch pen draws, one finger orbits, pinch zooms, long-press hints. `manifest.webmanifest` + `icon-180/192/512.png` + apple metas on topo and flight; `@media (display-mode: standalone)` pads the safe areas; hints speak touch on coarse pointers; video falls back to mp4. **Walker's iPad is the real test device** (Will's two are dead) and he relays her reports.
10. **Animate history (`5bd24d4`).** Plan view, sheet block. Every version of the satellite picture since 2014, played in order, each rising up the valleys over the last with the changed ground pulsing ember, the date stamped, yearly greenness bars, a slider with play/pause, save video. **Sources, all keyless and CORS-open:** Esri Wayback release list at `wayback.maptiles.arcgis.com/.../MapServer?f=json` (the S3 config json is stale at 2023), tiles at `.../tile/{release}/{z}/{y}/{x}`, and the **tilemap's `size[0]` is the change detector** — equal bytes means the same picture, so `histVersions()` keeps only releases where it changes (Encino Solo: 12 pictures, 2014-02 → 2025-09, ~10 s). EOX Sentinel-2 yearly mosaics `tiles.maps.eox.at/wmts/1.0.0/s2cloudless-{year}_3857/…` (2018–2025 here). USGS LandsatLook: 403, unusable from a browser. `rasterFrom(urlOf, ppc)` generalises buildAerial's mapping. A throw inside the loop is caught now (a negative first index once killed the page's rAF loop).
11. **Points, step one (`0d9c34b`).** `tools/cloud2bin.py` reads a textured OBJ (vertex colour from the texture at its first face corner), `.las/.laz` or `.ply` and writes **CPC1**: 80-byte header (count, origin lat/lon, min/range xyz, name) then 8 bytes a point (xyz u16 quantised, rgb 5-6-5). Encino Solo's 2019 drone mesh → 1,021,033 points, 8.2 MB, 14 s. In the tool, model block, deep: **points / no points / load**. `placeCloud()` maps every point to grid coordinates through `latLonToGrid`; `drawCloud()` writes specks straight into an ImageData through the model camera (nearer brighter, every fourth point while the camera moves, hidden in front of a cut). Kept in IndexedDB (`surveyStore` gained `putKey/getKey/delKey`), back after a reload once the terrain is in — **the lookup is deferred with `setTimeout(…, 0)` because the store is declared further down the file** (a TDZ throw broke the page once). One frame with a million points ≈ 245 ms; orbit stays usable.
12. **Hooke Park research.** Will's reference #1 for the point-cloud look. Brief published (link above), essentials in memory. Their clouds are of **objects** (204 forks surveyed → 25 → 20 for the Wood Chip Barn truss; photogrammetry, Arup script, robot milling; a LiDAR survey checked the built barn), never terrain, never atmosphere. The four rules I drew for us: the cloud as an inventory, dots on paper over line ground, cut the cloud with the section, check the built with a second survey.

## Open, waiting on Will

- **Tour copy.** He is still moving things around. The Drive docx (`INBOX (1)\coyote-tour-stops.docx`, 9 Sept 10:20) is **stale**: walk and the sculpt/draw split both changed cards. When he says he is done, rebuild it from the live TOUR with `scratchpad/tour-doc.py` (previous session's scratchpad), then his edits → respell → record.
- **The digital voice-over.** Pipeline proven, nothing recorded. Four test lines from the twin voice sit in the previous session's scratchpad awaiting his verdict. Record with `WebDev/twin` F5-TTS **in float32** (`vo-batch32.py`; fp16 is silent on the GTX 1660 Ti) → `vo-assemble.py` → `vo_check.py`. Narrated cut goes live only after he hears it; the silent cut is live now.
- **Which Hooke Park images he means** (forks on the bench, the barn's cloud, the robot films, the built pieces) → then points step two.
- **References for the animation** before anything else happens to it.
- Six example picks for the teachers page gallery; the OpenAI key pasted once in his browser; the Google Earth Pro installers downloaded.
- Walker's iPad report.
- Open the obj and glb in his CAD and say what it reports.

## Open, no dependency on Will

- **Points step two** (waits on his images): the ephemeral looks — depth fade to dissolve, dust over the white model, the section cut slicing the cloud, points settling into the ground, photo colour on points over line ground — and WebGL if millions at speed are wanted.
- **Animate history round 2**, his ask: further back than satellites from historical maps (USGS historical topo works for US sites; **Encino Solo is in Mexico**, where I found no keyless georeferenced service — probe David Rumsey and INEGI), forward into climate futures (the bathtub sea-rise model already exists, so a coastal site can get 2030/2050/2100 frames; inland needs another driver), and AI in-betweens for erosion and river course.
- USGS unified geology (no lat/lon endpoint yet); parametric drawing sheet; deeper geology sources; sandbox hardware test at USC; teachers page tuning.

## How to verify quickly

- Local: `cd coyote-studio && python -m http.server 8765`, then any `e2eNNN.js` with `node`, Playwright-core at `C:\Users\zolar\AppData\Roaming\npm\node_modules\@playwright\cli\node_modules\playwright-core`. `window.__dbg(fn)` hands out `S, AI, ANIM, HIST, CLOUD, camera, aiControl, aiCall, aiPrompt, snapshotProject, bakePainted, renderPicture, kmlOut, geoTiff, aerialCache, AER, loadCloud, animStart, animDraw, W, H` and more.
- Live: `curl -s https://coyotemountainfarm.com/topo.html | grep -q '<marker>'` in a loop; Pages takes 10–70 s.
- The artifact copy: strip `<!doctype html>`, `<html…>`, `<head>`, `</head>`, `<body>`, `</body>`, `</html>` lines and republish with `url`.
