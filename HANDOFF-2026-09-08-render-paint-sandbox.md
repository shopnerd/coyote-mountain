# Handoff: render, paint it, sandbox, tour, simple/deep, geology
Date: 2026-09-08. Written by Claude (Fable 5.1) for Will, and for whoever picks the studio up next (a later Claude session, or another model).

If you are reading this with no other context: everything you need is here. The topo tool is one file, `topo.html`, about 3,300 lines of vanilla JavaScript and Canvas, no libraries, in the public GitHub Pages repo `shopnerd/coyote-mountain`, live at https://coyotemountainfarm.com/topo.html. The earlier handoff `HANDOFF-2026-09-07-flight-survey.md` covers the flight planner and the drone-survey import and is still current.

## 1. What changed since the 9/7 handoff, in the order it landed

1. **Grading tools.** Pad (rectangle, oval, drawn shape; balanced cut and fill or held level; edges as slopes, walls where needed, or walls), path (centreline, corridor draped on the ground, raised or sunk, bare/gravel/paved), grade a→b now cuts and fills instead of pushing earth, retaining walls drawn and counted, building relief for milling (blocks, pedestal, recess, outline), cnc lines DXF in the project's units.
2. **Render mode** (model view → render). A section-perspective: free camera (eye level, low aerial, high), real sun by month and hour with cast shadows, sky (clear, cumulus, marine layer, overcast), the section cut along A–B as a black or white poché band that spans the whole frame with blank paper below it, or no cut, in which case the base slab and its sides are drawn so it reads as a model on a table. `renderSet()` makes a multi-view sheet.
3. **Paint it.** The render becomes the control image for an image model (Google Gemini image, model id in localStorage `gemini-model`, default `gemini-3.1-flash-image`; the key lives only in the browser's localStorage `gemini-key`, never in the repo). Five styles: photograph, watercolour, ink wash, map mash-up, white model (ground and buildings as white card with the contour steps, only planting and water painted; its control is forced to paper and no aerial reference is sent). The control frame is *structured*: contour lines drawn on the ground, standing rain pools in blue, the surface colour map you chose (elevation, slope, aspect, sun...) kept at full strength, no captions or credits, no blue tint in shadows. The prompt is written as a strict image-to-image edit and names everything the control carries. See section 3 for what the painter still gets wrong.
4. **Sandbox** (`sandbox/bridge.py`, `sandbox/README.md`). A depth camera over real sand drives the tool; projector window with keystone. Built against the Orbbec Femto Bolt through Orbbec's K4A wrapper DLLs, tested only with `--mode fake`. Real-hardware test is pending at USC.
5. **Guided tour**, 27 stops in deep, 17 in simple, ordered top to bottom: header, command row, then the rail in plan, model, section (with the geology stop), analysis, then world, site, grading, fabricate, exports. Steps marked `deep: true` are skipped in simple. The tour hands the user's own project back at the end.
6. **Simple vs deep** switch in the header. Simple keeps every tool whose result shows on the sheet in one gesture; deep adds tuning, calibration, fabrication and the extra exports. First visit is simple, remembered in localStorage `coyote-depth`. Implementation: class `deep` on about 58 elements plus one CSS rule `html[data-depth="simple"] .deep{display:none!important}`; hidden settings keep their values.
7. **Geology under the section.** A `geology` switch in the section view (deep). Samples nine points along A–B, asks Macrostrat (`/api/v2/geologic_units/map?lat&lng&scale=large|medium|small|tiny`, first non-empty scale wins, CORS open) for the mapped unit, draws bands under the existing ground hatched by rock type, with name, lithology and age. Depth is nominal (45% of the section relief) and the sheet says so. At Encino Solo: Neogene intermediate volcanics on the west, Late Cretaceous sandstone on the east.
8. **Model zoom** to 24× (was 6×); tiles subdivide when larger than 30 px on screen with bilinear heights, slopes and light, so deep zoom shades smoothly.
9. **Home project** is Will's clean Encino Solo sheet saved 2026-09-08 00:14 UTC (`home.json`).
10. Dark mode across the studio, landing page at the root, sketch studio hidden at `sketch.html` (unlinked, noindex, still in the public repo).
11. **Imported objects** (late 9/8). An `object` brush in plan (deep): import an STL (binary or text) or OBJ from any CAD program, pick its units, and it lands at the centre of the sheet centred on its footprint with its lowest point on the ground. Drag to move, tap empty ground to move the selected one there, sliders for rotation, scale and lift. Files over 40,000 faces are simplified by vertex clustering on the way in. The object is a stroke (`kind: 'obj'`, triangles in metres rounded to the millimetre, convex-hull footprint), so it saves, opens and undoes with the project. In the model and the render every face is a lit triangle in the painter's sort (big faces are quartered at placement so they sort against the ground tiles); it goes to the painter as "a designed structure called <name>" and rides along in the STL export at the same scale. Not yet: the section cut through it, a CNC pedestal from its footprint, cast shadows from it onto the ground.
12. **Painted sheet** (`painted sheet · pdf + png`, deep). The drawing sheet itself with two boxes painted: the model box carries the view as it stands and the box under the section carries the active section seen in perspective (camera square to a–b via `secYaw()`, low aerial, black cut; the south elevation only when there is no line), both from the painter in the current style; plan, analysis, section, notes and title block are drawn. `drawingSheet(painted)` takes `{ model, elev }` images; `paintSheet()` makes the two calls. `aiPaint` was split into `aiControl()` (the structured control + prompt), `aiCall()` (the request) and the panel. (An earlier four-times-of-day painted set was replaced by this; the plain render set was removed later the same night.)
14. **Several sections.** `S.secs` is a list of up to four A–B lines (letters A–B, C–D, E–F, G–H), `S.secI` the active one, and `S.sec` always mirrors the active line so every older consumer (render cut, geology, drawing sheet, pip, 3d jump) still works. Drag with the section tool for a new line, tap a line to make it active, tabs on the section page switch and remove, tapping a band on the page activates it, the page stacks all of them (active carries the geology and the accent), the pip and the drawing sheet show the active one alone. Saved as `secs` + `secI` (older files with `sec` still load). Undo restores the list.
15. **Reference photo for the painter** (render block, deep). `reference photo` loads any image (resized to 1024 px, jpeg), shown as a thumbnail with a clear button; a mode row says whether it is *the place* (materials, plants, ground colours, weather, light; never composition or landforms) or *the look* (medium, palette, brushwork, mood; never content). It goes to the painter as an extra image after the aerial, and the prompt names it with the right ordinal. Held in memory for the session only (`AI.photo`), not saved in the project. `aiCall` now takes an array of reference jpegs.
16. **Studio backdrops.** The sky row has three more choices: seamless (warm-grey sweep), white, charcoal. `drawSky` fills the frame with the sweep and returns (no horizon, no clouds); the prompt swaps the horizon clause for a "plain studio backdrop, keep it empty" clause and names the backdrop in the scene; captions say "seamless backdrop" etc. via `skyWord()`.
17. **Sheets for the plotter.** Both sheets render at 150 dpi (`RES = 1.5` in `drawingSheet`: the page context is scaled, plan views are rendered at 1.5x with type held, the vector views draw in their box's coordinates on a 1.5x canvas). Arch C comes out 3600x2700, arch D 5400x3600. The section box shows the active section alone (`drawSection(..., activeOnly)`), and short section bands keep their margins in proportion so the vertical never inverts. The painted sheet asks the painter for 2K pictures, and its second picture is a **true section**: camera square to the active line, eye level, orthographic, the line filling the frame, centred on its midpoint, black cut. (`window.__ctl` is a test hook that receives the second control's png.) On the painted sheet the painting takes the big top-left box and the plan moves to the small top-right box at its own scale (`swap`, `pBox`/`mBox`, `scP`/`kP`; dimension strings and scale bars follow the plan; the title block reads both scales).
18. **Render rail consolidated.** The section cut and the background are two `<select>` menus (`#cutSel`, `#skySel`, wrappers keep the ids `#cutRow`/`#skyRow`), the three eye heights moved into the command row's view buttons (`#views button[data-eye]`), and a **paint switch** (`#paintRow`, `S.paintOn`) shows the style, reference photo, words and paint-it rows only when on. Tests select with `selectOption('#cutSel', …)` and click `#paintRow button[data-pt="1"]` before painting.
19. **The cut is held after painting.** `aiControl()` also returns a `mask` canvas with just the cut face and the paper (drawn by `drawCutFace` from the same geometry); `holdCut(img, mask)` lays it back over the painter's picture, so the poché and the paper are always ours and the painter only ever touches the ground and sky. Used by paint it and by both pictures of the painted sheet. The painted section box crops around the cut line (`elevFocus`), more ground above than paper below.
20. **Render set removed** (Will, 9/8): the plain four-times-of-day sheet, its button, `renderSet()` and `composeSet()` are gone; the drawing sheet and the painted sheet are the two outputs.
21. **Two painter tics to keep** (Will's call): buildings occasionally hanging over the model's edge, and clouds drifting out past the model onto the backdrop. Both make the picture read as a real model on a table under a real sky. Do not add prompt wording or masks that suppress them.
22. **Aerial switch in the model menu.** The `#aerialRow` element travels with the mode in `setMode`: under smooth / stepped in the model view (padding 0 12px), back among the sheet switches after `#annoRow` in plan. Its state is still per view (`aeKey()`: `aerial` in plan, `aerialM` in the model).

## 7. State at the end of the night (9/8)

Live and verified (updated after item 27): grading tools, render mode with the held cut, paint it with five styles, structured control, reference photo, studio backdrops, painted sheet (painting big, plan small, true painted section), drawing sheet at 150 dpi, imported CAD objects, several sections with geology, simple/deep, the 27/17-stop tour, smooth deep zoom, the consolidated render rail. Removed: the render set. Pending: the sandbox with the Femto Bolt at USC; a depth-conditioned painter if Gemini's edits ever get loose again; deeper geology sources; and Will's idea for another day, a parametric drawing sheet whose boxes you drag bigger and smaller with the scales following.
23. **Section camera reads like the drawing.** `secYaw()` now picks the yaw where A and B share a depth *and A projects left of B*, so the 3D jump and the painted section read the same way as the drawn profile whichever way the line was dragged (Will saw one mirrored). `window.__dbg(fn)` exposes `{ S, camera, secYaw }` for tests only.
24. **Style row always with render.** The five styles show whenever render is on (the painted sheet uses the chosen style without the paint switch); the paint switch reveals the reference photo, the words and paint it / key.
25. **Sheet split is adaptive, both ways.** For both sheets the split searches the right column's width (38–50%) and the painting's height (50–72%) so the plan prints at the largest standard scale the paper allows, then the analysis (score = 2 × plan ratio + analysis ratio, tie to the taller plan box); the right column stacks to the same heights (plan + section = painting; painted section + notes = analysis) so edges align across the gutter; the section box is 230 px or 24% of the painting, the notes box 255–300 px, the rendered or painted section takes the rest. Arch C: plan 1 in = 1000 ft, analysis 2000 (the right column widens to half the page); arch D: plan 800, analysis 1000. `S.lastSheet` records the scales and box heights for tests. The painted sheet button sits directly above the drawing sheet button.
26. **One layout for both sheets.** The drawing sheet now follows the painted sheet's rules: the rendered model view big top-left, the plan small top-right at its own scale, the drawn section over the same section rendered (`sectionCamera()` + `renderPicture()`, the same camera the painter gets), compact notes, the adaptive split. `sheetPictures(model, elev, painted)` feeds `drawingSheet()`; titles read `MODEL · render` / `SECTION X–Y · render` on the plain sheet. `S.sheetPic` keeps captions off the sheet's pictures. The old "MODEL · analysis layers" and "ELEVATION from the south" boxes are gone (the south elevation remains only as the fallback when no section line exists).
27. **One sheet.** The plain drawing sheet is gone as a separate output. The primary **drawing sheet · pdf + png** button runs `paintSheet()`: the model and the active section painted (two calls, 2K), plan, analysis and notes drawn, the title block naming the style and model. With no Google AI key in the browser the two pictures come from the renderer instead (toast says so) and the title block reads coyote mountain studio. File names are `coyote-topo-sheet-…`. The `painted sheet` button is removed.
28. **Licence, teachers page, walkthrough** (9/8 morning). `LICENSE.md` is the PolyForm Noncommercial License 1.0.0 with the required notice (copyright William S. Rollins) and a plain-words preface. `teachers.html` is the page for landscape studios: what a session does, beside Grasshopper (a table), the walkthrough video, every tour stop as a frame with its caption, the sandbox, what students need, the licence. `walk/walkthrough.mp4` (2:44, 2.8 MB, 1280x720) is the 17-stop simple tour rendered from Playwright screenshots with ffmpeg; `walk/walk-NN.jpg` are the frames. `topo.html#tour` opens the tour on load. The landing page links to the teachers page and the licence.
29. **Drawing pinned to the ground** (9/8, Will: "pen drawings don't scale if you change the site width"). `remapDrawing(oldW, oldRot)` converts every stroke point and centre, section line, keypoint, dam point and grade line from the old frame to the new by way of metres east/north of the site centre, scales tree canopies by oldW/newW so they keep their size in metres, and turns imported objects with the rotation. `S.frameW`/`S.frameRot` remember the frame the drawing belongs to (stamped in `fetchWorld` and `loadProject`); the width and rotation `change` handlers remap before refetching. Also this morning: the status line trimmed to one clause per tool (navigation moved to the keys line, `KEYS_LINE`), view buttons numbered, bigger logo, the tour-stops Word document in the Drive inbox for Will's edits.
13. **See this section in 3d** (section page). Searches the camera yaw where A and B sit at the same depth with the site behind the plane, sets low aerial, perspective, black cut if none, and jumps to the render. The 3D section is the render itself; paint it from there.

## 2. How the paint pipeline works (so you can change it safely)

- `aiPaint()` sets `S.rendOn = true; S.mode = 'model'; S.aiCtl = true`, draws the model at 1536×864 into an offscreen canvas, draws rain pools on top with `drawRainOverlay(..., poolsOnly = true)` (pools on the camera side of the cut are skipped via `rendCutSide` / `camSideOf`), then resets the flags. `drawModel0` returns right after `rendCutSide = cutSide` when `S.aiCtl` is set, so nothing textual reaches the control.
- `aiPrompt()` builds the text from `AI.style`, `AI.ctl` (`pools`, `map`, `slab`), `aiScene()` (month, hour, sky, place, strokes, buildings, walls, the aerial ground-cover reading, and the user's words box) and the cut description.
- Requests: `POST /v1beta/interactions` first (`input: [text, image, image?]`, `response_format: image`), fallback `models/{model}:generateContent` with `inlineData` and `responseModalities: ['IMAGE']`. `findImg()` digs the base64 out of either shape. The aerial photo goes along as a second image when it is on, with wording "colour and texture only, never layout".
- The painted image element is created on demand inside `#aiImgWrap`; `#aiPanel` shows it with save png / again / close.

## 3. What the painter still gets wrong, and what to try next

- **Landmarks from the place name.** Told "Seattle" it added the Space Needle. Leave the place name out of the words box to stop it.
- **Water it wants to see.** A large blue area in the control is respected; a subtle one sometimes grows. The prompt already forbids water when the site has none.
- **Framing drift.** When the model is small in the frame the painter sometimes grows the terrain to the edges. Zoom so the model fills the frame before painting.
- **Next lever if control is still too loose:** a depth-conditioned image model (a second provider) with our depth map as a hard constraint. The renderer can output a depth map for free.

## 4. Testing and deploy method (keep doing this)

- Patch files are Python scripts with assert-before-replace (`rep()`), applied to `topo.html`; then `node -e` runs `new Function` over every `<script>` block as a syntax check.
- Playwright-core (`C:\Users\zolar\AppData\Roaming\npm\node_modules\@playwright\cli\node_modules\playwright-core`) end-to-end scripts run first against `python -m http.server 8765` in the repo folder, then against the live site after `git push` (GitHub Pages takes about 60 s; poll `curl | grep` for a new identifier).
- Live checks that exist in the scratchpad from this session: `e2e122` (deep tour, every stop's target visible), `e2e123` (render cut, zoom), `e2e124` (simple/deep, simple tour), `e2e125` (oblique cut, paper never above the band), `e2e126` (paint control and prompt with a mocked Gemini route), `e2e127` (no-cut slab), `e2e128` (geology against the real Macrostrat API), `e2e129` (white model style), `e2e130` (object import, drag, rotate, prompt, stl export, save, remove; uses `house.stl`/`house.obj` generated in the scratchpad), `e2e131` (visual: the house in axon and render), `e2e132` (section-in-3d jump; its painted-set half is superseded), `e2e134` (painted sheet with two mocked calls, png kept), `e2e135` (reference photo: load, thumbnail, ordinals with and without the aerial, place/look wording, clear), `e2e136` (studio backdrops: pixels, prompt clauses), `e2e137` (both sheets at 150 dpi with three sections and geology on, png kept), `e2e138` (the painted sheet's section control saved for a look), `e2e139` (aerial switch travels with the mode), `e2e140` (section camera keeps a left; style row with render), `e2e141` (sheet split on arch c and arch d), `e2e142` (the sheet without a key: drawn, no painter call), `e2e143` (teachers page, video, licence, tour from a link), `e2e144` (status line never clips), `e2e145` (drawing pinned through width and rotation changes); `walk-frames.js` regenerates the frames, then the ffmpeg concat in the transcript makes the video, `e2e133` (several sections: tabs, stacking, tap to activate, remove, undo, save, 3d follows the active).
- The artifact copy of `topo.html` at https://claude.ai/code/artifact/062c2198-f39e-45bc-aa65-6025b1e7b42c is republished after each change (outer document tags stripped).

## 5. Traps that bit this session

- **Never append a `//` comment to the end of a one-line JavaScript statement chain in `topo.html`.** It swallows the rest of the line. It broke the paint panel once and the whole render mode once (live for about ten minutes) before the live check caught it.
- The depth switch handler and the theme handler live *outside* the main IIFE: `toast()`, `resize()` and `S` are not in scope there.
- `S` and `TOUR` are not reachable from Playwright's `page.evaluate`; tests read the DOM instead.
- Tour targets must be elements that are always visible: `#bldRow`, `#bldModeRow`, `#seaRow`, `#roadRow`, `#ovRow`, `#trimRow` are conditional.
- The paper polygon under the cut must be extended along the band's own line, not horizontally, or a wedge of paper appears above the band on oblique cuts.

## 6. Open items

- Sandbox with the Femto Bolt at USC: `bin/k4aviewer.exe` first, then `python sandbox/bridge.py --mode k4a --dll "<wrapper>\bin"`, open http://localhost:8787/topo.html (the https site cannot fetch localhost).
- Geology: deeper sources (SGM Mexico 1:50,000 sheets, well logs, SoilGrids for soils) if real depths are wanted.
- Depth-conditioned painting (section 3) if Gemini's edits stay too free.
- The sketch studio at `sketch.html` needs work before it is linked again.

30. **World block in plan view only** (late 9/8). `setMode` hides `#worldLbl` + `#worldSec` unless the mode is plan. Tour stops for the world block already set plan mode, so nothing else moved.
31. **Tilt-shift test** run live through the words box (photograph style) rather than code: result convincing (sharp band, blur top and bottom, richer colour). Not in `aiPrompt()` yet; add as a switch or style only if Will says yes.

32. **Rail cuts (9/9, commit `72231ed`, tag `rail-cuts`; the state before is tag `rail-before-cuts`).** Rule: one home per tool (1 plan draws, 2 model looks, 3 section cuts, 4 analysis asks). Removed: the render's own month/hour (the sun block is the one clock, shown with render on, its shadows/sun-hours row only for the study), model surface colours sun + cut·fill, the flood layer (storm rain slider moved into the analysis params because the rain animation uses it), ½/2× width, random ground + invented hills relief, reset grade, the model's rain pause, low/high eye heights. Merged: plowing effect sits on the keyline row and only shows with keyline on. Moved: hachures, labels, cut·fill wash, read uphill, aerial photo + opacity into a `#sheetSec` block (plan only). Deep counts plan 80→77, model 60→49, section 36→35, analysis 50→48. Tour texts adjusted (render, surface colour, analysis, site). Walkthrough frames and the teachers page still show the old rail: regenerate with the tour rewrite. **Revert in one line:** `git revert --no-edit rail-cuts` then push. Review page: https://claude.ai/code/artifact/62eb87de-f9e6-41fb-8b6a-df320c77d462

33. **Tour follows the new rail (9/9, `27ed0c0`).** Order: top row (2) · 1 plan top to bottom: views, world, lot/survey, flight rows, brushes, pen, planting, grade, pad, path, object, section line, sheet block · 2 model: model, render, paint, surface colour, sculpt · 3 section: section (+ paint this section), geology · 4 analysis · shared: site, interval, grading, fabricate, exports. 28 stops deep, 17 simple; `setRain(false)` now lives on the site stop. Verified every stop lands on a visible element (e2e150).
34. **Paint this section (9/9).** `#sec3d` (label "paint this section") calls `paintSection()`: `sectionCamera()` (the drawing sheet's rule: square to the active line, eye level, no perspective, line fills the frame, black cut) → `aiControl()` → `aiCall(..., '2K')` → `holdCut` → the painted panel via the new `showPainted(src, title, note, name, again)`; `AI.again` / `AI.lastName` make again and save png act on whatever was painted last. No key: `renderPicture()` drawn instead, title "section A–B · drawn". The old behaviour (jump to model view at a low aerial) is gone. Live test with Will's key: photo style, cut face held; the painter put distant hills behind the profile despite the prompt (same tic as the model view).
35. **Walkthrough regenerated** from the new simple tour: `walk/walk-01..17.jpg`, `walk/walkthrough.mp4` (2 min 45 s, 9 s a frame, last 12), teachers page cards rewritten from `walk-captions.json`. Narrated cut still pending Will's voice verdict + docx edits.

36. **Rail sections (9/9, commit `bbd3773`, tag `rail-sections`; before = tag `rail-before-sections`).** One-word headings, the same skeleton in every view. 1 plan: view · world · draw (was brush) · sheet (now holds contour interval + hachures) · 2 model: model (smooth/stepped, photo, orbit/sculpt, rain) · render (switch, cut, background, surface colour, sun clock) · paint (heading `#paintLbl`, shown with render on) · 3 section: sections · geology · 4 analysis: water (all layers but sun, rain, sliders) · sun (`#sunRow` with the sun study button + `#sunSlot`; `setMode` moves `#sunSec` into the slot in analysis and back after `#colorRow` otherwise; `anaClick` handles both rows) · shared: site · grading · fabricate · project (`#project`: undo, delete all, save, new/open, copy link) · exports (`#exports`: png, stl, laser, dxf, cnc, drawing sheet). CSS: `#projLbl{margin-top:auto}`. Tour: 28 deep / 18 simple, model order model → sculpt → render → colour → paint, `#interval` stop folded into the sheet and site stops, `.actions` stop split into project + exports. **Revert in one line:** `git revert --no-edit rail-sections` (the cuts stay; `git revert --no-edit rail-cuts` takes those out too).
37. **Walkthrough + teachers page** regenerated for 18 stops (video 2 min 54 s). **Tour doc on Drive** rebuilt from the live TOUR by `scratchpad/tour-doc.py` → `Google Drive\INBOX (1)\coyote-tour-stops.docx` (CARD + VOICE per stop, DROP / ADD AFTER n conventions); `vo-script.txt` now 18 voice lines. Next: Will's edits → apply → respell → record (float32) → vo_check → narrated cut.

38. **Reference photos, plural (9/10, `9da3950`).** `AI.photos` (max `AI_REF_MAX` = 3), each `{data, url, name, mode}` with mode `place | look | building`; `#aiRefFile` is `multiple`; `#aiRefList` draws one row per photo (thumb, name, role seg, x); `aiPrompt()` writes one clause per photo in order after the aerial (ordinals second..fifth); the building clause: wherever the view shows a building or a placed object, paint it as this building, take nothing else from the photo, no building where the view shows none. `aiControl` sends `[aerial, ...photos]`. `window.__dbg` now also hands out `aiPrompt, snapshotProject, cs, applyPad, objFromMesh, updateStats, W, H` for tests.
39. **Centro Equino (Walker's equestrian centre) on the topo.** Built by `scratchpad/e2e152.js` from the annotated Google Earth image (0.175 m/px, barn at pixel 1240,690 = 32.000240, -116.763689, site width 520 m): parcel + turnout as dashed pen polygons, the arena as a level pad 182 x 78 ft at bearing 23 + outline, the round pen 60 ft, six 12 x 40 ft runs NW of the barn, the barn as a placed object (gable, 72 x 40 ft, eave 12, ridge 19, `rot` 48). Project: `Google Drive\INBOX (1)\coyote-topo-centro-equino.json` (open it with the open button). First live paint (photo style, barn render as "the building", horses via the words box, no cut, hour 15, camera yaw 2.4 pitch 1.15 zoom 4.6): `INBOX (1)\centro-equino-painted-01.jpg`. Note the painter kept the dashed parcel lines on the ground; if unwanted, hide pen strokes before painting (later: a "hide lines" switch for the painter).

40. **Centro Equino corrected (9/10, `ac9d7be`).** Will's point 32.000240, -116.763689 is beside the ROUND PEN, not the barn: the layout is now anchored on the Google Earth graticule (pixel 1009,757 = the point; 0.181 m/px from the scale bar; barn at pixel 1240,690). The first build also ran before the terrain fetch finished (ground came out 850-1000 ft and steep); e2e152 now waits for the fetch button to read `fetch terrain` again. Real ground at the parcel: ~1075 ft, nearly flat; hills rise to ~1250 ft to the east. Pad cut = fill 340 yd³.
41. **Open a project from a link.** `topo.html?proj=projects/centro-equino.json` fetches a saved project kept beside the page (path must match `[\w./-]+\.json`) and loads it before the place/home logic; the copy-link toast now says the drawing and grading travel as a saved file, not in the link. `projects/centro-equino.json` is committed to the PUBLIC repo for Walker's link (Will can pull it if he minds). Share link for Walker: https://coyotemountainfarm.com/topo.html?proj=projects/centro-equino.json
42. **"Terrain exaggerated" answer.** Vertical exaggeration slider default 1.5 applies to the model and render; 1.0 is true to the 30 m elevation data. The real-place look came from: aerial photo draped (aerial on) so the painter takes ground cover from the imagery, mapped water off (`raw ground`, the OSM water blobs are wrong here), VE 1, hour 15, words "gentle valley floor, flat open dry pasture, hardly any rocks, vineyard rows beyond". Result: `INBOX (1)\centro-equino-painted-04.jpg`. The painter still invents one small shed near the round pen.

43. **Backlog (9/11).** (a) Analysis layers: topographic wetness index and geomorphons (ten landform classes), both from the fetched ground; LandScope sells them by name. (b) Geology: the USGS is unifying its geologic maps (GeMS schema, National Geologic Map Database); Macrostrat is what the section uses today; revisit when USGS publishes a seamless service with an API. (c) First-person walk: a walk mode in the model (eye height on the ground, WASD + look, terrain following, the aerial or the painted ground as texture) is a modest addition to the existing renderer; an AI walkthrough video from the painted stills (image-to-video, Higgsfield connector is on this machine) is the quick version; an image-to-3D-world model is the fancy version. (d) Exports at true coordinates: GeoTIFF of the graded ground + kmz of the drawing. Plan artifact updated with the LandScope market note; Drive copy too.

44. **Walk mode (9/11, `ffa32eb` + `92d4b8b`).** Third button in the model block's orbit/sculpt row: `walk`. `walkStart()` stands the eye at the model centre, 1.7 m up, level (pitch 1.5, orbit's pitch means pi/2 = horizontal), render on. `walkProj(w,h)` is a first-person projection with the same yaw/pitch axes as the orbit camera (forward = (sin yaw, cos yaw) in east/south), 70 deg FOV, points behind the eye return NaN (a NaN path draws nothing); `camera()` and `drawModel0` swap to it when `S.walk` is set (the pip keeps the orbit view); `walkHorizonY` feeds `drawSky`. Keys: w a s d / arrows move, q e or side arrows turn, shift runs (4 or 12 m/s), a drag looks (orbit() with wider pitch range). rAF loop `walkTick`, lowQ while moving (~10 fps in headless Chromium), full quality at rest. No section cut on foot. Leaving the model view or pressing orbit/sculpt calls `walkStop()`. Cache keys carry `walkKey()`.
45. **Painted ground.** After a paint, a `painted` button appears beside aerial photo / no photo. `bakePainted()` re-projects the last painting (`AI.lastImg`, camera in `AI.lastCam` saved by `aiPaint`/`paintSection`) onto the ground raster at the photo's resolution (AER px per cell) with a depth test (tiles drawn far-to-near into a depth canvas, quarter-metre steps; hidden ground keeps the aerial or paper). `paintedCache.canvas` then rides the aerial texture path (`groundPainted()`), so you can walk inside the painting. The bake takes ~0.2 s; the switch reports how much ground the picture covered.
46. **The photo grid, fixed.** The faint white lattice Will saw on the photo ground had three causes, all fixed: the per-tile shade polygon left unshaded antialiased seams (now overlaps by .6 px); the tone fill under a patch was outset by OUT and lighter than the photo (no outset under patches now); and on foot the affine 3-corner patch missed the trapezoid's fourth corner (patches larger than 40 px are now two exact affine triangles, clipped with a hair of overlap).
47. **Walk-through video.** `scratchpad/walk-video.js` (this session) records 200 frames of a scripted walk on the painted ground (project from the link, real painting `equino-painted-flat.jpg` baked with its camera yaw 2.4 / pitch 1.15 / zoom 4.6, pip hidden) and ffmpeg makes `centro-equino-walkthrough.mp4` (8.3 s, 24 fps, 1280 px). Copy in the Drive share folder. **AI video route:** Higgsfield free plan (9.4 credits) refuses every video model ("requires basic plan or higher"); the painted still is uploaded there as media 01aee5f9-8347-484c-8881-6abbc0d42f24 if Will upgrades.
48. **Next (parked at Will's request 9/11).** (a) Live proof of paint → painted → walk in Will's Chrome with his key. (b) Objects are not painted in the bake (the barn stays a white box on foot): bake object faces too, or paint from the walk camera. (c) 2D drawing animation, Will's next idea: pie charts and species data sets drawn in, a person walking through a section, birds through a scene, shadows sweeping, ambient sound; keep it simple. (d) Regenerate walkthrough frames/teachers cards (the sculpt tour stop text changed). (e) LiDAR answer given: US sites already use USGS 3DEP (LiDAR-derived, resampled server-side); elsewhere 30 m tiles; drone survey fills the gap.

49. **Second painter: OpenAI (9/12, `861ec15`).** A `painter` menu (`#aiEngine`, deep) above paint it · key: google · gemini flash image (default), openai · gpt-image-1-mini (~1c), gpt-image-2 (~5c), gpt-image-2.5 sunburst (~25c); choice kept in localStorage `coyote-painter`. The key button asks for the key of the chosen painter: `gemini-key` or `openai-key`, browser only, never in the repo. `aiCall()` hands OpenAI engines to `aiCallOpenAI()`: the 1536 x 864 control is letterboxed to 1536 x 1024 with its own top and bottom rows (a sentence tells the painter to leave the bands), posted as multipart to `https://api.openai.com/v1/images/edits` with the references as further `image[]` entries, `quality` medium (high for the 2K sheet), `input_fidelity: high` first and the same request again without it when the model rejects the flag (gpt-image-1-mini and gpt-image-2 both reject it), `data[0].b64_json` cropped back to 1536 x 864, then `holdCut` and the panel as before. Titles say the model. **CORS:** OpenAI's 200 and 400 answers carry `access-control-allow-origin: *`; their 401 does not, so a wrong key surfaces as a plain "openai refused the request" toast, by design. Verified: request shape from Node with the environment key (`scratchpad/openai-paint-test.js`, control dumped by e2e157), the browser path end to end with a wrong key (e2e158). gpt-image-2 held the model (slab, layout, horses); mini invented a new scene. Not yet run in Will's own browser: he pastes the key with the key button once.
